{
  "name": "wallet-connect-example-vue",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server:node": "cd backend-nodejs && npm install && npm start",
    "server:python": "cd backend-python && python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt && python3 server.py"
  },
  "dependencies": {
    "vue": "^3.5.18",
    "wallet-connect-button-vue": "^1.0.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.1.0"
  }
}
